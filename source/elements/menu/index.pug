mixin menu(baseClass, menuData, preselectItem)
	-var menuItems = menuData || menu
	ul(class=baseClass+'-list')
		each val, index in menuItems
			if index == preselectItem
				li(class=baseClass+'-list_item')
					a.scroll_spy_link.scroll-link(href="#"+val[1] class=baseClass+'-list_item-link active')=val[0]
			else
				li(class=baseClass+'-list_item')
					a.scroll_spy_link.scroll-link(href="#"+val[1] class=baseClass+'-list_item-link ')=val[0]
		li(class=baseClass+'-list_item')
			a.menu-button(href=menu_button[1], target="_blank")=menu_button[0]

header
	.header_inner_wrap
		.header_inner
			a.scroll-link(href="#"+small_logo.href).logo
				+insert-image(small_logo.src, 'logo', 'logo-img')
			button.toggle-menu-button
			nav.header_menu#header_menu
				+menu("header_menu", menu)
